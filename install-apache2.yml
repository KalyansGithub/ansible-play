---
- name: install packages on webservers
  hosts: dbservers
  become: true
  tasks:
  - name: install apache2
    apt:
      name: apache2
      state: present
    notify: start apache2
  handlers:
  - name: start apache2
    service:
      name: apache2
      state: started


